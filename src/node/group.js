import Element from './element';
import Attr from '../attribute/group';

export default class extends Element {
  static Attr = Attr;

  appendChild(el) {

  }

  replaceChild(el) {

  }

  removeChild(el) {

  }

  insertBefore(el) {

  }
}